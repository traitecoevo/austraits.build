```{r, echo=FALSE, results="hide"}
key <- data_study$key
```

# Report for study: `r key`

## Contact Information

**Data contributor:** `r data_study$metadata$data_contributor`

## Overview of data provided

The dataset includes records for `r nrow(data_study$data)` individuals from `r length(unique(data_study$data$species_name))` species belonging to ... with data included for the following variables `r length(unique(data_study$data$trait_name))`: `r paste(unique(data_study$data$trait_name), collapse = ", ")`.


